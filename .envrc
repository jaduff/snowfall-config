#echo 'Enter passphrase to unlock ssl key'
#read -s SSH_TO_AGE_PASSPHRASE; export SSH_TO_AGE_PASSPHRASE
export SOPS_AGE_KEY=$(ssh-to-age -i ~/.ssh/id_ed25519 -private-key)
export EDITOR=/run/current-system/sw/bin/vim
